# p2a 사이클 회고: buildlore v15

- 기간: 2026-08-24 ~ 2026-08-24
- 실행 모드: orchestrated
- 총 run 수: 6
- 작성 시점: iteration close 직전

> **에이전트 작성 규칙**: 각 항목 옆 `[출처]` 표기된 아티팩트에서 사실만 수집해 채운다.
> 판정이 필요한 항목(C1·E1)은 근거를 제시하고 `사용자 확정 필요`로 남긴다.
> 증거가 없으면 빈 칸으로 둔다. 추측으로 채우지 않는다. 이 파일 외부를 수정하지 않는다.

## A. 동작 정합성 — 의도대로 되었나

### A1. 라우팅이 틀렸던 순간
`p2a next`가 잘못된 상태를 판단하거나 엉뚱한 명령을 권장한 적이 있는가?
(없으면 "없음")

- 없음. active iteration의 task-001~task-006이 각각 정확히 한 run과 연결되고 6개 run 모두 `finished`로 기록됐다. `[출처: runs/run-index.json]`

### A2. 검증 오탐·부작동
validate/verify가 통과 못 시켰어야 할 것을 통과시켰거나, 정상인 걸 막은 적이 있는가?

- 기록된 오탐·부작동 없음. 55개 verification이 모두 `passed`, `exitCode=0`이다. task-003 이후 `p2a doctor --target . --dev`는 manifest `0.5.10`과 실행 runtime `0.5.11` 차이를 경고했지만 실패로 처리하거나 정상 진행을 막지는 않았다. `[출처: 6개 run의 verification[]]`

### A3. 데이터 손실
prune·삭제로 나중에 필요한 증거가 사라져 후회한 순간이 있는가?

- 없음. run-index의 6개 `runRef`와 대응 run 파일 6개가 모두 남아 있다. `[출처: runs/run-index.json, runs/v15-agent-harness-compile-backend/*.json]`

## B. 성능 — 느렸던 것

### B1. 체감상 느렸던 명령 `[출처: run 파일 verification[].durationMs 상위 집계]`
명령명과 대략 소요 시간. "없음"도 답이다.

- `npm test`: 6회, 평균 26.201초, 최대 28.040초. 최대값은 `run-2026-08-24T12-13-15-206Z-task-004`에서 기록됐다.
- `npm run lint`: 6회, 평균 7.894초, 최대 8.716초.

### B2. 반복 대기 `[출처: 위 집계에서 동일 명령 반복 여부]`
매번 같은 명령에서 기다린 경험이 있는가? (익숙해져서 놓치기 쉬운 누적 비용)

- 각 task closeout에서 같은 검증 묶음을 반복했다. `npm test` 6회의 누적 시간은 157.204초이고, `npm run lint` 6회는 47.362초다. spec 검증과 task-graph 검증도 각각 6회 실행되어 누적 15.326초와 15.196초를 사용했다.

## C. 게이트 가치 — 승인 경계가 일했나

### C1. 실제로 잡은 문제 `[출처: run.interruptions[] gate_return + assessment, 사용자 판정 필요]`
Gate A/②/B 또는 완료 조건에서, 그 경계가 없었다면 잘못된 상태로 넘어갔을 명확한 순간.

- 사용자 확정 필요. 6개 run의 `interruptions[]`에 `gate_return` 기록이 0건이어서 아티팩트만으로 판정할 근거가 없다.

### C2. 도장만 찍은 승인 `[출처: gate_return 목록 중 assessment=valid가 아닌 것 / 승인 직후 수정 없음]`
기여 없이 형식으로만 통과한 승인. 어느 게이트였는지.

- **사용자 응답 (2026-08-24)**: 승인할 때 용어·문장·질문이 너무 어려워서 무슨 말인지
  이해하지 못한 채 승인한 경우가 있었다. 승인 프롬프트가 시스템 용어 중심이라
  처음 접하는 사람이 판단할 수 없었다. → 개선 방향: ELI5 방식(전체 그림 먼저,
  예/아니오 결과 명시, 실행 명령 제시, 기술 세부는 별도 섹션)으로 승인 질문 재작성.

## D. 우회와 마찰

### D1. 하네스를 피해 손으로 한 작업
정식 절차 대신 터미널에서 직접 처리한 것과 이유.

-

### D2. 자유 메모
- 가장 아팠던 마찰 1가지: 사용자 확정 필요. 근거 후보는 task마다 전체 검증 묶음을 반복해 `npm test`만 누적 157.204초 사용한 점이다.
- 예상 밖으로 좋았던 것 1가지: 사용자 확정 필요. 근거 후보는 6개 task가 각각 단일 run에서 `finished`됐고 interruption·실패·재시도가 모두 0건인 점이다.

## 수집 요약 (에이전트 기록란)

> 아래 숫자는 작성 시점 아티팩트에서 계산한 값이다.

| 지표 | 값 | 출처 |
| --- | --- | --- |
| 총 run 수 / 상태 분포 | 6 / finished 6 | runs/run-index.json |
| gate_return 횟수 (valid/invalid) | 0 (0/0) | run.interruptions[] |
| user_correction 횟수 | 0 | run.interruptions[] |
| implementation_interruption 횟수 | 0 | run.interruptions[] |
| 검증 실행 횟수 / 평균·최대 durationMs | 55회 / 평균 4,949ms·최대 28,040ms | run.verification[] |
| 실패·재시도 run 수 | 실패 0 / 재시도 0 | run-index status 집계 |

## E. 다음 사이클 결정

### E1. 하네스 동결 여부
다음 사이클은 하네스 수정 없이 제품에 집중하는가? (Y/N)

- 사용자 확정 필요 (Y/N 미확정).

### E2. 동결 해제 시 최우선 수정 1개
E1이 N일 때만. 딱 하나만.

- 후보 (사용자 확정 필요): 승인 질문 평이 언어화 — Gate A/②/B 프롬프트를
  "한눈에(무엇을 결정하나+예/아니오 결과) → 실행 명령 → 세부 계약" 구조로 재작성.

---

> 작성 규칙: 추측 금지, 근거(run id / 명령명) 우선, 모르면 비움.
> 판정 규칙은 원본 템플릿과 같은 디렉터리의 `README.md`를 따른다.
